# Bash Script: Delete Files Containing Digits in Name

## Problem Statement
This script (`script3.sh`) deletes all **files** in the current directory whose names contain at least one digit.

⚠️ Important:
- Only regular files are deleted
- Directories are never deleted, even if their names contain digits

---

## Example

### Initial directory:
